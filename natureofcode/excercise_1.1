var x = 100; Variables for location and speed of ball.
var y = 100;
var xspeed = 1;
var yspeed = 3.3;
//Remember how Processing works? setup() is 
// executed once when the sketch startsand draw() 
// loops forever and ever (until you
quit).
void setup() {
size(640,360);
background(255);
}
void draw() {
background(255);
x = x + xspeed; Move the ball according to its speed.
y = y + yspeed;
if ((x > width) || (x < 0)) { Check for bouncing.
xspeed = xspeed * -1;
}
if ((y > height) || (y < 0)) {
yspeed = yspeed * -1;
}
stroke(0);
fill(175);
ellipse(x,y,16,16); Display the ball at the location (x,y).